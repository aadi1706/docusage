# Minimal dependencies for evals/ragas_eval.py
# Does NOT include torch/colpali/whisper — those are for ingestion only

# Orchestration (needed by agents/graph.py imports)
langgraph==0.2.28
langchain==0.3.7
langchain-community==0.3.7
langchain-openai==0.2.8

# Vector DB
qdrant-client==1.11.3
sentence-transformers==3.3.1
rank_bm25>=0.2.2
Pillow>=9.2.0,<11.0.0

# Eval (ragas installed --no-deps in CI; its langchain<0.3 pin is too strict
#        but the public API works fine against langchain 0.3.x at runtime)
datasets==3.1.0
pysbd>=0.3.4
appdirs>=1.4.4

# LLOps
langfuse==2.57.4

# API / utils
pydantic==2.9.2
python-dotenv==1.0.1
loguru==0.7.2
tqdm==4.67.0
pandas>=2.0,<3
numpy>=1.24,<2
httpx==0.27.2
pypdf==5.1.0
openai==1.55.3
